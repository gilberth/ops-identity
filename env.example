# ==============================================
# AD Sentinel - Environment Configuration Guide
# ==============================================
# Copy this file to .env and fill in your values.
# DO NOT commit .env to version control.

# ----------------------------------------------
# 1. AI Configuration (REQUIRED for Analysis)
# ----------------------------------------------
# Provider: 'openai' or 'lovable' (default: openai)
AI_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-your-key-here
AI_MODEL=gpt-4o-mini

# Lovable Configuration (if using lovable)
# LOVABLE_API_KEY=your-lovable-key

# ----------------------------------------------
# 2. Deployment Configuration (Used by scripts)
# ----------------------------------------------
# DigitalOcean API Token (can also be exported in shell)
# DO_API_TOKEN=dop_v1_...

# GitHub Personal Access Token (for pulling images from GHCR)
# GITHUB_TOKEN=ghp_...

# ----------------------------------------------
# 3. Application Configuration (Runtime)
# ----------------------------------------------
# Port for the Node.js server (default: 3000)
PORT=3000

# Database Connection String
# For local dev: postgres://postgres:postgres@localhost:5432/active_scan_db
# For production: Injected automatically by deployment script
DATABASE_URL=postgres://postgres:postgres@localhost:5432/active_scan_db

# Frontend API Endpoint
# Leave EMPTY for production (uses relative paths /api/...)
# Set to http://localhost:3000 for local dev if frontend/backend are separate
VITE_VPS_ENDPOINT=

# ----------------------------------------------
# 4. Security & Tuning
# ----------------------------------------------
# JWT Secret (if auth is implemented)
# JWT_SECRET=change_me_in_production

# Node Environment
NODE_ENV=development
