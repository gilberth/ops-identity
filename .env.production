# Production environment variables for VPS deployment
# These are compiled into the build at build time

# Backend API endpoint - uses nginx reverse proxy
# Empty string because nginx already routes /api/* to backend
VITE_VPS_ENDPOINT=http://157.230.138.178
